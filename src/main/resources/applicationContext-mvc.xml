<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 注解驱动 -->
    <mvc:annotation-driven/>

    <!-- 静态资源配置 -->
    <mvc:default-servlet-handler/>

    <!-- 视图解析器 -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!--注册拦截器-->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/admin/**"/><!--拦截/**下的所有方法-->
            <!--<mvc:exclude-mapping path="/admin/login"/>-->
            <bean class="com.merrill.web.interceptor.AdminTokenInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <mvc:view-controller path="/user/" view-name="user/views/repairmentApply"/>
    <mvc:view-controller path="/operator/" view-name="operator/views/orderListView"/>
    <!--<mvc:view-controller path="/" view-name="user/user/login"/>-->

</beans>